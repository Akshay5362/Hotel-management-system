# Firebase Admin SDK Configuration Template (HPMS-Sky5)
# Copy these keys into your local `backend/.env` file and populate with actual credentials.
# DO NOT commit real secrets or private keys to source control.

FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=
